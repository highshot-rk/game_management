# $.fn.formatUserList = (options) ->
#   user_data = $(this).attr(options.attribute)
#   user_data = JSON.parse(user_data)
#   $(this).html(user_data.join(', '))

# $(document).on 'ready page:load', ()->
#   $('[data-event-subscribers]').formatUserList({ attribute: 'data-event-subscribers' })


