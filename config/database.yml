# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: 10
  timeout: 5000
  username: admin
  password: admin
  host: localhost

development:
  <<: *default
  database: freankexpo_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: freankexpo_test

production:
  <<: *default
  adapter: postgresql
  username: postgres
  password: passw0rd
  host: db
  database: freankexpo
